// Per-release download page behavior (OS tabs, arch switching, copy buttons).
// Reuses the exact same logic as the homepage download section so the two
// stay in sync.
import '../parts/download.js'
