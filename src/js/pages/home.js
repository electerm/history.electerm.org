// Home page entry.
//
// The archive landing page is deliberately static: no analytics, no hero
// canvas, no ad networks. The only behavior it needs is the mobile nav toggle,
// which every page on the site shares via ./parts/header.js.
import '../parts/header.js'
